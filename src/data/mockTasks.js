// TODO(sesion-04): reemplaza este arreglo por al menos 4 tareas de ejemplo,
// cada una con { id, title, status }, usando status: "pendiente" | "en_progreso" | "completada".
export const mockTasks = [
  // { id: 1, title: '{{REEMPLAZAR}}', status: 'pendiente' },
];
